// tools/selectTool.ts — stub for now, Phase 3 will fill this in
import { AppState } from '../lib/types';
import { ToolHandler } from './index';

export const selectTool: ToolHandler = {
  onPointerDown(state, x, y) { return {}; },
  onPointerMove(state, x, y) { return {}; },
  onPointerUp(state, x, y)   { return {}; },
};